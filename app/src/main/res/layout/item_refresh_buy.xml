<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_rent_refresh_tab">

    <TextView
            android:id="@+id/tv_text"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:gravity="center"
            android:text="hello"
            android:textSize="15sp"
            android:textColor="@color/color_rent_refresh_tab"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    <View
            android:id="@+id/line_mark"
            android:layout_width="0dp"
            android:layout_height="3dp"
            android:background="#FF7500"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="@id/tv_text"
            app:layout_constraintStart_toStartOf="@id/tv_text" />

    <ImageView
            android:id="@+id/tv_selected"
            android:layout_width="30dp"
            android:layout_height="16dp"
            android:src="@drawable/yishe"
            app:layout_constraintStart_toEndOf="@id/tv_text"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_marginTop="2dp"
            android:visibility="gone"/>
</androidx.constraintlayout.widget.ConstraintLayout>